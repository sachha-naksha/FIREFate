"""Command-line entry points, one module per SLURM step.

Each is runnable as ``python -m focalfire.cli.<name>``; the batch scripts under
``multiome_dynamic_regulation/bash_scripts/`` call them that way.
"""
